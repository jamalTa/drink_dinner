{% extends 'base.html.twig' %}

{% block title %}Supplement{% endblock %}

{% block body %}
    <h1>Supplement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ supplement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ supplement.nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ supplement.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_supplement_index') }}">back to list</a>

    <a href="{{ path('app_supplement_edit', {'id': supplement.id}) }}">edit</a>

    {{ include('supplement/_delete_form.html.twig') }}
{% endblock %}
