{% extends 'base.html.twig' %}

{% block title %}Condiment{% endblock %}

{% block body %}
    <h1>Condiment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ condiment.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ condiment.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_condiment_index') }}">back to list</a>

    <a href="{{ path('app_condiment_edit', {'id': condiment.id}) }}">edit</a>

    {{ include('condiment/_delete_form.html.twig') }}
{% endblock %}
